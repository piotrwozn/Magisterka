# Frontend production env
# Point at the deployed backend (Spring Boot Gateway behind Nginx)
VITE_API_URL=https://api.sorai-triage.com

# Set to "true" to force offline demo mode (no backend calls)
VITE_USE_MOCK=false
