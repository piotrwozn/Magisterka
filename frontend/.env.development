# Frontend development env
# Default: talk to local Spring gateway on 8080, or set "http://localhost:8000"
# to bypass the gateway and hit FastAPI directly.
VITE_API_URL=http://localhost:8080

# Toggle offline demo mode without restarting
VITE_USE_MOCK=false
